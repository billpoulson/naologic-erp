# Future / Upgrade Todos

| Todo | When | Notes |
|------|------|-------|
| Angular 21 upgrade | May 2026 | When Angular 21 enters LTS |
| Test categorization | Soon | Add tags/categories to separate longer-running browser automation (Playwright) tests from fast unit tests; e.g. `@e2e` / `@slow` so CI or dev can run `test` vs `test:e2e` selectively |
| Short work orders in month view | TBD | Determine how to handle display of short work orders in month view (e.g. single-day or few-day orders may be too narrow to show name/label; consider minimum width, truncation, tooltip, or icon-only) |
